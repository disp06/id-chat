# Secret key for Flask sessions
SECRET_KEY=super-secret-change-in-production

# Host and port (used if running without docker-compose)
HOST=0.0.0.0
PORT=5537

# Redis connection URL (optional, for future scaling)
# REDIS_URL=redis://localhost:6379/0
